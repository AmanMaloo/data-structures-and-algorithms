int BS(vi &arr,int x){
    int low = 0; 
    int high = arr.size()-1;
    int ans;
    while(low<=high){
       int mid = (low+high)/2;
       if(){
           ans = mid;
           low = mid + 1;
          }
       else high = mid - 1;
     }
}
