int numberOfSubs(vector<int>&arr,int target){
   // only valid if all numbers in arr are positive
     vector<int>dp(target+1);
     dp[0] = 1;
     for(int i = 1 ; i <= x ; i++){
       for(int j = 0 ; j < n ; j++){
          if(arr[j] <= i ) {
              dp[i] += dp[i-arr[j]]; 
              dp[i]%=mod;
          }
        }
      }
    return dp[target];
}
